import s from './Setting.module.css'

const Setting = () => {
  return (
    <div className={s.wrapper}>
      <div className={s.title}>
        The dark theme will be available later.
      </div>
    </div>
  )
}

export default Setting